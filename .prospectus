Prospectus.extra_dep('file', 'prospectus_circleci')

item do
  noop

  deps do
    name 'gems'
    expected do
      static
      set 'green'
    end

    actual do
      gemnasium
      slug 'halyard/boxen'
    end
  end

  extend ProspectusCircleci::Build.new('halyard/boxen')
end
